# Local MongoDB for development - copy to .env and adjust as needed.
# The API reads MONGODB_URI from apps/api/.env; keep the values in sync
# (user, password, database) so the connection string matches.
MONGO_INITDB_ROOT_USERNAME=deepa
MONGO_INITDB_ROOT_PASSWORD=deepa_pass
MONGO_DATABASE=deepa
MONGO_PORT=27017